{
    "version": 2,
    "buildCommand": "npx vite build -c vite.config.ts",
    "outputDirectory": "dist/public",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}